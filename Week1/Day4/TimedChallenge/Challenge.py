# Write a program which takes a string and a character as an input, and finds out the number of occurrences the character has in the string.

text = input('String: ')
chr = input('Character: ')[0]
print (text.count(chr))